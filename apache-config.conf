<VirtualHost *:80>
    # 🔥 CRÍTICO: Debes apuntar la raíz (DocumentRoot) a la carpeta 'public'
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        # Permitir configuraciones y reescrituras de URL
        AllowOverride All
        # Permite el acceso
        Require all granted
    </Directory>

    # Configuración de logs para Fly.io
    ErrorLog /dev/stderr
    CustomLog /dev/stdout combined
</VirtualHost>
